<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 应用自更新交给系统安装器；Android 8+ 仍需用户授予“安装未知应用” -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Shizuku 后端：由 Shizuku 服务校验，未装时无副作用 -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <!--
      后台跑任务期间 app 进程必须活着：进程一死，特权进程的看门狗就自杀并释放虚拟屏
      实测 MIUI 的 ProcessManager SwipeUpClean 会按 Adj=905 直接 force-stop
      前台服务本体是 .service.RunForegroundService，通知与电池白名单是它的前提
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!--
      定时任务
      USE_EXACT_ALARM 不申请：那是给闹钟类应用的，本应用不符合 Play 的准入描述
      没有 SCHEDULE_EXACT_ALARM 时退到 setAlarmClock，见 ScheduleAlarmManager
    -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!--
      前台模式的控制层：屏幕让给了目标应用，没有悬浮球或音量键就没法停任务
      两项都由特权进程代授（AppOps / Settings.Secure），用户不必逐个去系统页点
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 存储访问：与上面两项一样由特权进程代授，对齐 MaaMeow -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="AllFilesAccessPolicy,ScopedStorage" />

    <uses-permission
        android:name="android.permission.BIND_ACCESSIBILITY_SERVICE"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".MaaFwApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="${appIcon}"
        android:label="${appLabel}"
        android:roundIcon="${appRoundIcon}"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaaFwApp"
        android:localeConfig="@xml/locales_config"
        tools:targetApi="33">

        <!-- Sentry 的 SentryInitProvider 会在进程起来时自读 manifest 里的 dsn 初始化，
             而 DSN 只有加载完 PI 才知道；不关掉它，装上就是启动即崩（DSN is required） -->
        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />

        <!-- 只有启动 Activity 挂启动主题；application 那层保持 Theme.MaaFwApp，
             否则别的窗口也会顶着首屏底色起来 -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout"
            android:exported="true"
            android:launchMode="singleTop"
            android:supportsPictureInPicture="true"
            android:theme="@style/Theme.MaaFwApp.Starting"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- API 32- 的 per-app locale 由 appcompat 持久化并在 Activity 附着前回放（33+ 走系统） -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!--
          执行期间的保活前台服务
          specialUse：现有 FGS 类型没有一个对得上「经提权服务驱动另一块屏跑自动化」，
          dataSync / mediaProjection 都不是这回事，硬套会在 34+ 被判类型不符
        -->
        <service
            android:name=".service.RunForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps the app process alive while the privileged service drives a MaaFramework run" />
        </service>

        <!-- 只过滤按键，不读窗口内容；用途见 AccessibilityHelperService -->
        <service
            android:name=".service.AccessibilityHelperService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_helper_service_config" />
        </service>

        <receiver
            android:name=".schedule.ScheduleReceiver"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="${applicationId}.SCHEDULE_TRIGGER" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".schedule.ScheduleBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".schedule.ScheduleExecutionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Handles a scheduled trigger woken by an exact alarm" />
        </service>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            tools:ignore="ExportedContentProvider" />

        <!-- Root 后端的 binder 回投口：特权进程经 getContentProviderExternal 打到这里，
             provider 内按 uid ∈ {0, 2000} 与一次性 token 校验（docs/privileged-runtime.md §3） -->
        <provider
            android:name=".root.RootServiceBootstrapProvider"
            android:authorities="${applicationId}.root.bootstrap"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            tools:ignore="ExportedContentProvider" />

        <!-- 把随包的 shizuku.apk 交给系统安装器：API 24 起 file:// 直接抛 FileUriExposedException -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>

    <queries>
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

</manifest>
